# Meridian Benchmark Results

> Generated by `npm run benchmark` on 2026-06-07T15:22:56.054Z (Node v26.0.0).
> Reproduce with: `npm run benchmark`

All scenarios run **in-process against deterministic `MockAdapter`s** — no network
is involved, so results are reproducible and isolate Meridian's own overhead from
network variance. "Raw SDK" is the baseline you get calling a vendor SDK directly:
a single provider, no retry, no failover, no circuit breaker.

## Reliability checks

| Check | Result |
| --- | --- |
| 10,000 requests tracked in 1.223s | ✅ pass |
| OpenAI outage recovered via failover in 27ms | ✅ pass |
| Stripe 429 automatically retried | ✅ pass |
| Circuit breaker opened after 5 failures | ✅ pass |
| Schema drift detected before deployment | ✅ pass |

## Headline: same failures, different outcomes

| Scenario | Raw SDK | Meridian |
| --- | --- | --- |
| OpenAI outage | ❌ Fail | ✅ Success |
| Stripe 429 | ❌ Fail | ✅ Success |
| Network timeout (circuit open) | every call hits dead upstream | fail-fast in 648.9 µs |
| Schema drift | silent breakage | ✅ Detected |
| Added latency | 0 ms | +0.11 ms |

## 1. Throughput

10,000 requests through the full pipeline in
1.223s. Analytics tracks every request.

- Added overhead per call: **+0.11 ms**
- Raw fetch + parse: **0.01 ms**

## 2. Failover (OpenAI outage → Anthropic)

- Routing overhead: **0.08 ms / call**
- Recovery time (including fallback RTT): **27.15 ms**
- Meridian recovered: **100% of requests**
- Raw SDK (no failover): **failed on every call**

## 3. Retry (Stripe 429)

One rate-limit error then success. Raw SDK fails on the first attempt; Meridian retries.

- Raw SDK succeeded: **no**
- Meridian succeeded: **yes**

## 4. Circuit breaker

Always-failing upstream with a simulated **25 ms** round-trip.

- Circuit opened after **5 failures**
- Once OPEN: fail-fast in **648.9 µs** — saves ~25 ms per blocked call

## 5. Schema drift detection

- Detected removed field `customer_name`: **yes**
- Severity: **ERROR**

## Pipeline overhead breakdown

Per-configuration throughput (10,000 iterations each):

| Configuration | ops/s | µs/op |
| --- | ---: | ---: |
| provider.get() — no retry, no policy | 11,263 | 88.8 |
| provider.get() — 1 policy (allow-all) | 16,754 | 59.7 |
| service.get() — failover, 1 provider | 14,771 | 67.7 |

> **Note:** the ordering within this table is a V8 JIT artefact. The meaningful
> takeaway is that **all configurations run in under 100 µs per call**. See the
> throughput section for the headline overhead figure.

---

*Numbers vary with hardware and Node version. Re-run `npm run benchmark` to
generate figures for your environment.*
